# دليل ربط الأجهزة المختلفة بقاعدة بيانات مشتركة (Horizon HR)

يرشدك هذا الدليل لتوصيل الهواتف الذكية (Android)، وأجهزة الكمبيوتر المختلفة في المؤسسة بنفس الخادم وقاعدة البيانات المشتركة عبر الشبكة المحلية (Wi-Fi / LAN).

---

## 📌 المخطط العام للاتصال
1. **جهاز الخادم الرئيسي (Server PC):** يحتوي على قاعدة البيانات (MySQL) وتشغيل التطبيق الخلفي (Hono Server).
2. **أجهزة العميل (Clients):** الهواتف الذكية (تطبيق الأندرويد) أو أجهزة الكمبيوتر الأخرى تتصل بالخادم الرئيسي مباشرة عن طريق عنوان الـ IP الخاص به في الشبكة المحلية.

---

## 🛠️ خطوات الإعداد والربط

### الخطوة 1: معرفة عنوان IP لجهاز الخادم
على جهاز الكمبيوتر الرئيسي (الذي يعمل كخادم):
1. افتح قائمة ابدأ واكتب `cmd` لفتح موجه الأوامر.
2. اكتب الأمر التالي ثم اضغط Enter:
   ```cmd
   ipconfig
   ```
3. ابحث عن **IPv4 Address** تحت كارت الشبكة المتصل (مثل `Wireless LAN adapter Wi-Fi` أو `Ethernet adapter`).
4. سيكون العنوان بالشكل التالي تقريباً: `192.168.1.100` (قم بكتابة وحفظ هذا الرقم).

---

### الخطوة 2: السماح بالاتصال عبر جدار حماية ويندوز (Firewall)
لكي تسمح للأجهزة الأخرى بالوصول إلى الخادم، يجب فتح المنفذ (Port 3000) في جدار الحماية لجهاز الخادم:
1. افتح **لوحة التحكم (Control Panel)** -> **جدار حماية ويندوز (Windows Defender Firewall)**.
2. اضغط على **الإعدادات المتقدمة (Advanced Settings)** على اليسار.
3. اختر **Inbound Rules** ثم اضغط على **New Rule...** على اليمين.
4. اختر **Port** ثم اضغط Next.
5. حدد **TCP** وفي خانة **Specific local ports** اكتب: `3000`.
6. اختر **Allow the connection** ثم Next.
7. تأكد من تحديد كل الخيارات (Domain, Private, Public) ثم Next.
8. قم بتسمية القاعدة باسم `Horizon HR Port` ثم اضغط Finish.

---

### الخطوة 3: تهيئة ملف البيئة (.env) وبناء التطبيق للأندرويد
لجعل تطبيق الأندرويد يعرف أين يرسل الطلبات بدلاً من الاتصال محلياً بالهاتف:
1. افتح ملف `.env` في المجلد الرئيسي للمشروع.
2. أضف أو عدّل المتغير `VITE_API_URL` ليشير إلى عنوان الخادم الرئيسي والمنفذ `3000` كالتالي:
   ```env
   VITE_API_URL=http://192.168.1.100:3000/api/trpc
   ```
   *(ملاحظة: استبدل `192.168.1.100` بالـ IP الفعلي لخادمك الذي حصلت عليه في الخطوة 1).*

3. قم بإعادة بناء المشروع ومزامنته مع الأندرويد لتطبيق الإعداد الجديد:
   ```bash
   npm run build
   npx cap sync android
   ```

---

### الخطوة 4: تشغيل الخادم الرئيسي
تأكد من تشغيل الخادم على جهازك الرئيسي لكي يكون جاهزاً لاستقبال الطلبات:
* لتشغيل وضع التطوير:
  ```bash
  npm run dev
  ```
  *(سيكون الخادم متاحاً للشبكة بأكملها على المنفذ 3000 لأن الإعدادات مضبوطة لاستقبال الاتصالات من كافة العناوين `0.0.0.0`)*.

---

### الخطوة 5: ربط الهواتف والأجهزة الأخرى
* **هواتف الأندرويد:** تأكد من اتصال الهاتف بنفس شبكة الـ Wi-Fi المتصل بها الخادم الرئيسي. قم بتثبيت ملف الـ APK وتشغيل التطبيق وسيقوم تلقائياً بجلب البيانات من قاعدة البيانات المشتركة على الخادم.
* **أجهزة الكمبيوتر الأخرى:** افتح المتصفح على أي جهاز كمبيوتر متصل بالشبكة واكتب في شريط العنوان:
  ```
  http://192.168.1.100:3000
  ```
  *(استبدل الـ IP بـ IP الخادم وسيفتح النظام معك مباشرة بنفس قاعدة البيانات)*.
