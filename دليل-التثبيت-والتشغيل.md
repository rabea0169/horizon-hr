# دليل تثبيت وتشغيل نظام Horizon HR

## 📋 المحتويات
1. [متطلبات النظام](#متطلبات-النظام)
2. [التثبيت الأولي](#التثبيت-الأولي)
3. [إعداد قاعدة البيانات](#إعداد-قاعدة-البيانات)
4. [تشغيل المشروع](#تشغيل-المشروع)
5. [بناء تطبيق الأندرويد](#بناء-تطبيق-الأندرويد)
6. [الربط بين التطبيقات](#الربط-بين-التطبيقات)

---

## 🔧 متطلبات النظام

### البرامج المطلوبة:
- **Node.js** (الإصدار 18 أو أعلى) - [تحميل](https://nodejs.org)
- **npm** أو **pnpm** (يأتي مع Node.js)
- **MySQL Server** (الإصدار 8.0 أو أعلى) - [تحميل](https://dev.mysql.com/downloads/mysql/)
- **Git** (اختياري) - [تحميل](https://git-scm.com)
- **Android Studio** (لبناء تطبيق الأندرويد) - [تحميل](https://developer.android.com/studio)
- **Java Development Kit (JDK)** 17 أو أعلى

### التحقق من التثبيت:
```bash
node --version
npm --version
mysql --version
```

---

## 📦 التثبيت الأولي

### الخطوة 1: نسخ المشروع
```bash
# انتقل إلى المجلد الذي تريد حفظ المشروع فيه
cd C:\Users\YourUsername\Desktop

# انسخ مجلد المشروع من الموقع الحالي
# أو استخدم git إذا كان المشروع على GitHub
```

### الخطوة 2: تثبيت المتطلبات
```bash
# افتح موجه الأوامر (CMD) أو PowerShell
# انتقل إلى مجلد المشروع
cd path\to\app

# ثبت جميع المتطلبات
npm install

# أو استخدم pnpm (أسرع)
pnpm install
```

### الخطوة 3: إنشاء ملف البيئة
```bash
# انسخ ملف المثال
copy .env.example .env

# ثم عدل الملف .env بالمعلومات الصحيحة
```

---

## 🗄️ إعداد قاعدة البيانات

### الخطوة 1: إنشاء قاعدة البيانات

```bash
# افتح MySQL Command Line Client أو استخدم MySQL Workbench

# أنشئ قاعدة بيانات جديدة
CREATE DATABASE horizon_hr CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;

# أنشئ مستخدم جديد (اختياري - للأمان)
CREATE USER 'hr_user'@'localhost' IDENTIFIED BY 'strong_password_here';
GRANT ALL PRIVILEGES ON horizon_hr.* TO 'hr_user'@'localhost';
FLUSH PRIVILEGES;
```

### الخطوة 2: تحديث ملف .env

عدّل ملف `.env` وأضف معلومات الاتصال:

```env
# Database Configuration
DATABASE_URL=mysql://hr_user:strong_password_here@localhost:3306/horizon_hr

# Backend Configuration
APP_ID=horizon-hr-app
APP_SECRET=your_secret_key_here

# Frontend (OAuth - اختياري)
VITE_KIMI_AUTH_URL=https://auth.kimi.com
VITE_APP_ID=your_app_id

# Backend (OAuth - اختياري)
KIMI_AUTH_URL=https://auth.kimi.com
KIMI_OPEN_URL=https://open.kimi.com
```

### الخطوة 3: تشغيل الهجرات (Migrations)

```bash
# توليد ملفات الهجرة من schema
npm run db:generate

# تطبيق الهجرات على قاعدة البيانات
npm run db:push
```

---

## 🚀 تشغيل المشروع

### الطريقة الأولى: تطبيق Electron (Desktop)

```bash
# في وضع التطوير
npm run dev

# ثم في نافذة أخرى
npm run electron
```

### الطريقة الثانية: تطبيق الويب (Browser)

```bash
# بناء المشروع
npm run build

# تشغيل الخادم
npm run start
```

ثم افتح المتصفح على: `http://localhost:3000`

### الطريقة الثالثة: تطبيق محمول (Capacitor)

```bash
# بناء الويب أولاً
npm run build

# نسخ الملفات إلى Capacitor
npx cap sync

# فتح Android Studio
npx cap open android
```

---

## 📱 بناء تطبيق الأندرويد

### الخطوة 1: إعداد بيئة Android

```bash
# تأكد من تثبيت Android Studio و JDK

# ثبت Capacitor CLI عالمياً (اختياري)
npm install -g @capacitor/cli

# أو استخدم npx (لا يتطلب تثبيت عالمي)
```

### الخطوة 2: إضافة منصة Android

```bash
# أضف منصة Android إلى المشروع
npx cap add android

# أو إذا كانت موجودة بالفعل
npx cap sync android
```

### الخطوة 3: فتح في Android Studio

```bash
# افتح المشروع في Android Studio
npx cap open android
```

### الخطوة 4: بناء وتشغيل

في Android Studio:
1. اختر جهاز محاكي أو جهاز حقيقي
2. اضغط على `Run` أو استخدم `Shift + F10`
3. سيتم بناء التطبيق وتثبيته على الجهاز

### الخطوة 5: بناء ملف APK للتوزيع

```bash
# في Android Studio:
# Build → Build Bundle(s) / APK(s) → Build APK(s)

# أو من سطر الأوامر:
cd android
./gradlew assembleRelease
```

---

## 🔗 الربط بين التطبيقات

### 1. تكوين الخادم (Backend)

جميع التطبيقات (Desktop, Web, Mobile) تتصل بنفس الخادم Hono على المنفذ 3000.

**ملف `capacitor.config.ts` - تكوين الاتصال:**

```typescript
// للتطوير: استخدم عنوان IP الجهاز
server: {
  url: 'http://192.168.1.100:3000',  // استبدل بـ IP جهازك
  cleartext: true,
  allowNavigation: ['localhost', '192.168.1.*', '10.0.0.*'],
}

// للإنتاج: استخدم الملفات المحلية
server: {
  // اتركه فارغاً
}
```

### 2. تحديد عنوان IP الجهاز

```bash
# على Windows (CMD)
ipconfig

# ابحث عن "IPv4 Address" تحت "Ethernet adapter" أو "Wi-Fi"
# مثال: 192.168.1.100
```

### 3. تحديث ملف الإعدادات للأندرويد

عدّل `capacitor.config.ts`:

```typescript
const config: CapacitorConfig = {
  appId: 'com.horizonsolutions.hr',
  appName: 'Horizon HR',
  webDir: 'dist/public',
  server: {
    url: 'http://192.168.1.100:3000',  // ضع عنوان IP جهازك هنا
    cleartext: true,
    allowNavigation: ['localhost', '192.168.1.*', '10.0.0.*', '*.kimi.page'],
  },
  // ... الإعدادات الأخرى
};
```

### 4. مزامنة التطبيق

```bash
# بعد تعديل الإعدادات
npx cap sync android

# ثم افتح في Android Studio
npx cap open android
```

---

## 🔐 حسابات الدخول الافتراضية

| المستخدم | كلمة المرور | الدور |
|---------|----------|------|
| admin | admin123 | مدير النظام |
| supervisor | super123 | المشرف |
| accountant | acc123 | محاسب |
| worker | work123 | عامل |

**ملاحظة:** هذه الحسابات الافتراضية موجودة في `api/auth-router.ts`. في الإنتاج، يجب نقل المستخدمين إلى قاعدة البيانات.

---

## 📊 هيكل قاعدة البيانات

المشروع يستخدم **Drizzle ORM** مع **MySQL**. قاعدة البيانات تحتوي على أكثر من 80 جدول تغطي:

- **إدارة المستخدمين والأدوار** (Users, Roles, Permissions)
- **إدارة الموارد البشرية** (Employees, Departments, Attendance, Leaves)
- **إدارة الرواتب والمستحقات** (Payroll, Deductions, Allowances)
- **إدارة الإنتاج** (Production Models, Stages, Tasks)
- **إدارة المخزون** (Inventory, Products, Stock Movements)
- **إدارة المبيعات والشراء** (Sales, Purchases, Invoices)
- **إدارة الحسابات** (Accounts, Transactions, Reports)

---

## 🐛 استكشاف الأخطاء

### المشكلة: "DATABASE_URL is required"
**الحل:** تأكد من وجود ملف `.env` وأنه يحتوي على `DATABASE_URL` الصحيح.

### المشكلة: "Cannot connect to MySQL"
**الحل:**
1. تأكد من تشغيل MySQL Server
2. تحقق من بيانات الاتصال (username, password, host, port)
3. تأكد من وجود قاعدة البيانات

### المشكلة: "Port 3000 is already in use"
**الحل:**
```bash
# غيّر المنفذ في vite.config.ts أو استخدم متغير البيئة
set PORT=3001
npm run start
```

### المشكلة: "Android emulator cannot reach localhost"
**الحل:** استخدم عنوان IP الجهاز بدلاً من localhost في `capacitor.config.ts`

---

## 📚 موارد إضافية

- [Vite Documentation](https://vitejs.dev)
- [React Documentation](https://react.dev)
- [Hono Documentation](https://hono.dev)
- [Drizzle ORM Documentation](https://orm.drizzle.team)
- [Capacitor Documentation](https://capacitorjs.com)
- [Electron Documentation](https://www.electronjs.org/docs)

---

## 💡 نصائح مهمة

1. **للتطوير:** استخدم `npm run dev` للحصول على Hot Module Replacement (HMR)
2. **للإنتاج:** استخدم `npm run build` ثم `npm run start`
3. **قاعدة البيانات:** احفظ نسخة احتياطية قبل تطبيق الهجرات
4. **الأندرويد:** استخدم جهاز محاكي بدلاً من الجهاز الحقيقي في البداية لتسريع التطوير
5. **الأمان:** غيّر `APP_SECRET` و `DATABASE_URL` في الإنتاج

---

## 📞 الدعم

إذا واجهت أي مشاكل:
1. تحقق من السجلات (Logs) في Console
2. ابحث عن الخطأ على Google أو Stack Overflow
3. تحقق من توثيق المكتبات المستخدمة
4. اطلب المساعدة من فريق التطوير

---

**آخر تحديث:** يونيو 2026
**الإصدار:** 1.0.0
