# ملخص البدء السريع - Horizon HR

## ⚡ الخطوات الأساسية (5 دقائق)

### 1️⃣ التثبيت
```bash
npm install
```

### 2️⃣ إعداد قاعدة البيانات
```bash
# عدّل .env بمعلومات MySQL
npm run db:generate
npm run db:push
```

### 3️⃣ التشغيل
```bash
# الطريقة الأولى: المتصفح
npm run build
npm run start

# الطريقة الثانية: Electron
npm run electron
```

---

## 📱 بناء تطبيق الأندرويد

```bash
# بناء الويب
npm run build

# مزامنة Capacitor
npx cap sync android

# فتح في Android Studio
npx cap open android

# ثم اضغط Run (Shift+F10)
```

---

## 🔗 ربط الأندرويد مع الخادم

عدّل `capacitor.config.ts`:
```typescript
server: {
  url: 'http://192.168.1.100:3000',  // ضع IP جهازك هنا
}
```

---

## 🔑 حسابات الدخول

| المستخدم | كلمة المرور |
|---------|----------|
| admin | admin123 |
| supervisor | super123 |
| accountant | acc123 |
| worker | work123 |

---

## 📁 الملفات المهمة

| الملف | الوصف |
|------|-------|
| `.env` | إعدادات قاعدة البيانات |
| `capacitor.config.ts` | إعدادات الأندرويد |
| `package.json` | المتطلبات والأوامر |
| `db/schema.ts` | هيكل قاعدة البيانات |

---

## 🆘 استكشاف الأخطاء

| المشكلة | الحل |
|--------|------|
| "DATABASE_URL is required" | عدّل ملف `.env` |
| "Cannot connect to MySQL" | تأكد من تشغيل MySQL Server |
| "Port 3000 is already in use" | غيّر المنفذ أو أغلق التطبيق السابق |
| "App cannot reach server" | تأكد من عنوان IP في `capacitor.config.ts` |

---

## 📚 الأدلة الكاملة

- **دليل التثبيت:** `دليل-التثبيت-والتشغيل.md`
- **دليل MySQL:** `دليل-إعداد-MySQL.md`
- **دليل الأندرويد:** `دليل-تطوير-الأندرويد.md`
- **دليل الربط:** `دليل-الربط-والتكامل.md`
- **دليل البناء والتوزيع:** `دليل-بناء-وتوزيع-الأندرويد.md`

---

## 🚀 الأوامر المهمة

```bash
# التطوير
npm run dev              # تشغيل مع Hot Reload
npm run build            # بناء الإنتاج
npm run start            # تشغيل الإنتاج

# Electron
npm run electron         # تشغيل تطبيق Desktop

# قاعدة البيانات
npm run db:generate      # توليد الهجرات
npm run db:migrate       # تطبيق الهجرات
npm run db:push          # تطبيق التغييرات

# Capacitor
npx cap sync android     # مزامنة الملفات
npx cap open android     # فتح في Android Studio
npx cap add android      # إضافة منصة Android

# الأندرويد
./gradlew assembleRelease   # بناء APK
./gradlew bundleRelease     # بناء App Bundle
```

---

## 🎯 خطوات التطوير

1. **التطوير المحلي:**
   - استخدم `npm run dev`
   - اختبر في المتصفح أو Electron

2. **الاختبار على الأندرويد:**
   - استخدم `npm run build`
   - ثم `npx cap sync android`
   - اختبر على محاكي أو جهاز حقيقي

3. **النشر:**
   - بناء APK أو App Bundle
   - توقيع التطبيق
   - رفع على Google Play

---

## 💡 نصائح

- استخدم `npm run dev` للتطوير السريع
- اختبر على أجهزة مختلفة قبل النشر
- احفظ نسخة احتياطية من قاعدة البيانات
- استخدم Git لتتبع التغييرات
- اقرأ السجلات (Logs) عند حدوث أخطاء

---

**آخر تحديث:** يونيو 2026
