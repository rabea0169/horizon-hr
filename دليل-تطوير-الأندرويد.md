# دليل تطوير تطبيق Android - Horizon HR

## 📋 المحتويات
1. [متطلبات التطوير](#متطلبات-التطوير)
2. [إعداد بيئة Android](#إعداد-بيئة-android)
3. [بناء التطبيق](#بناء-التطبيق)
4. [الربط مع قاعدة البيانات](#الربط-مع-قاعدة-البيانات)
5. [الاختبار والنشر](#الاختبار-والنشر)

---

## 🔧 متطلبات التطوير

### البرامج المطلوبة:

1. **Android Studio** (الإصدار الأخير)
   - [تحميل](https://developer.android.com/studio)
   - يشمل: Android SDK, Emulator, Build Tools

2. **Java Development Kit (JDK)** 17 أو أعلى
   - [تحميل](https://www.oracle.com/java/technologies/downloads/)
   - أو استخدم JDK المدمج في Android Studio

3. **Node.js** (الإصدار 18 أو أعلى)
   - [تحميل](https://nodejs.org)

4. **Git** (اختياري)
   - [تحميل](https://git-scm.com)

### التحقق من التثبيت:

```bash
# Java
java -version

# Node.js
node --version
npm --version
```

---

## 🚀 إعداد بيئة Android

### الخطوة 1: تثبيت Android Studio

1. حمّل Android Studio من الرابط أعلاه
2. شغّل المثبّت واتبع التعليمات
3. عند الانتهاء، ستُطلب منك تثبيت Android SDK
4. اختر الإصدارات التالية:
   - **API Level:** 34 أو أعلى (أحدث إصدار)
   - **API Level:** 24 (الحد الأدنى للتوافقية)

### الخطوة 2: إعداد متغيرات البيئة

**على Windows:**

1. افتح `System Properties` (اضغط `Win+Pause`)
2. اضغط على `Environment Variables`
3. أضف متغيرات جديدة:
   - `ANDROID_HOME`: `C:\Users\YourUsername\AppData\Local\Android\Sdk`
   - `JAVA_HOME`: `C:\Program Files\Java\jdk-17` (أو مسار JDK الخاص بك)

4. أضف إلى `PATH`:
   - `%ANDROID_HOME%\platform-tools`
   - `%ANDROID_HOME%\tools`

5. أعد تشغيل CMD

**على macOS/Linux:**

```bash
# أضف إلى ~/.bash_profile أو ~/.zshrc
export ANDROID_HOME=$HOME/Library/Android/Sdk
export PATH=$PATH:$ANDROID_HOME/platform-tools
export JAVA_HOME=$(/usr/libexec/java_home)

# ثم أعد تحميل الملف
source ~/.bash_profile
```

### الخطوة 3: التحقق من الإعداد

```bash
# تحقق من Android SDK
adb --version

# تحقق من Java
java -version

# تحقق من Gradle
gradle --version
```

---

## 🏗️ بناء التطبيق

### الخطوة 1: بناء الويب

```bash
# في مجلد المشروع
npm run build
```

### الخطوة 2: إضافة منصة Android

```bash
# إذا لم تكن موجودة
npx cap add android

# أو مزامنة الملفات
npx cap sync android
```

### الخطوة 3: فتح في Android Studio

```bash
# افتح المشروع في Android Studio
npx cap open android
```

### الخطوة 4: بناء التطبيق

في Android Studio:

1. انتظر انتهاء Gradle sync (قد يستغرق دقائق)
2. اذهب إلى `Build` → `Build Bundle(s) / APK(s)` → `Build APK(s)`
3. انتظر انتهاء البناء (قد يستغرق عدة دقائق)

---

## 🔗 الربط مع قاعدة البيانات

### 1. تحديد عنوان IP الخادم

أولاً، احصل على عنوان IP جهازك:

**على Windows:**
```bash
ipconfig
```

ابحث عن `IPv4 Address` تحت `Ethernet adapter` أو `Wi-Fi`
مثال: `192.168.1.100`

**على macOS/Linux:**
```bash
ifconfig
```

### 2. تحديث ملف الإعدادات

عدّل `capacitor.config.ts` في جذر المشروع:

```typescript
import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.horizonsolutions.hr',
  appName: 'Horizon HR',
  webDir: 'dist/public',
  server: {
    // للتطوير: استخدم عنوان IP جهازك
    url: 'http://192.168.1.100:3000',  // ⚠️ استبدل بـ IP جهازك
    
    // للإنتاج: اتركه فارغاً
    // url: '',
    
    cleartext: true,
    allowNavigation: ['localhost', '192.168.1.*', '10.0.0.*', '*.kimi.page'],
  },
  android: {
    backgroundColor: '#1a202c',
    allowMixedContent: true,
    buildOptions: {
      keystorePath: undefined,
      keystoreAlias: undefined,
    },
  },
  plugins: {
    SplashScreen: {
      launchAutoHide: true,
      backgroundColor: '#1a202c',
      androidScaleType: 'CENTER_CROP',
    },
  },
};

export default config;
```

### 3. مزامنة التطبيق

```bash
# مزامنة الملفات
npx cap sync android

# ثم افتح في Android Studio
npx cap open android
```

### 4. إعدادات الاتصال الإضافية

تأكد من أن جهازك (أو المحاكي) والخادم على نفس الشبكة:

**للمحاكي:**
```bash
# إذا كان الخادم على localhost:3000
# استخدم 10.0.2.2 بدلاً من localhost
# لأن 10.0.2.2 يشير إلى host machine من داخل المحاكي

# في capacitor.config.ts:
url: 'http://10.0.2.2:3000',
```

**لجهاز حقيقي:**
```bash
# استخدم عنوان IP الفعلي لجهازك
url: 'http://192.168.1.100:3000',
```

---

## 🧪 الاختبار والنشر

### اختبار على محاكي

1. في Android Studio، اضغط على `AVD Manager` (الرمز الأيسر في الأعلى)
2. اختر جهاز محاكي أو أنشئ واحداً جديداً
3. اضغط على زر التشغيل (▶️) لبدء المحاكي
4. في Android Studio، اضغط `Run` أو `Shift+F10`
5. اختر المحاكي وانتظر التثبيت

### اختبار على جهاز حقيقي

1. **تفعيل Developer Mode:**
   - اذهب إلى `Settings` → `About phone`
   - اضغط على `Build number` 7 مرات
   - ستظهر رسالة "Developer mode enabled"

2. **تفعيل USB Debugging:**
   - اذهب إلى `Settings` → `Developer options`
   - فعّل `USB Debugging`

3. **توصيل الجهاز:**
   - وصّل الجهاز بكابل USB
   - في Android Studio، اضغط `Run`
   - اختر جهازك من القائمة

### بناء APK للتوزيع

```bash
# في Android Studio:
# Build → Build Bundle(s) / APK(s) → Build APK(s)

# أو من سطر الأوامر:
cd android
./gradlew assembleRelease
```

الملف الناتج: `android/app/release/app-release.apk`

### بناء App Bundle للـ Google Play

```bash
# في Android Studio:
# Build → Build Bundle(s) / APK(s) → Build Bundle(s)

# أو من سطر الأوامر:
cd android
./gradlew bundleRelease
```

الملف الناتج: `android/app/release/app-release.aab`

---

## 🔐 التوقيع الرقمي (Signing)

### إنشاء مفتاح التوقيع

```bash
# استخدم Android Studio
# Build → Generate Signed Bundle / APK
# ثم اتبع الخطوات

# أو من سطر الأوامر:
keytool -genkey -v -keystore my-release-key.keystore -keyalg RSA -keysize 2048 -validity 10000 -alias my-key-alias
```

### استخدام المفتاح

في `android/app/build.gradle`:

```gradle
signingConfigs {
    release {
        keyAlias 'my-key-alias'
        keyPassword 'your_key_password'
        storeFile file('path/to/my-release-key.keystore')
        storePassword 'your_store_password'
    }
}

buildTypes {
    release {
        signingConfig signingConfigs.release
    }
}
```

---

## 📱 ميزات Capacitor

### الوصول إلى ميزات الجهاز

Capacitor يوفر API للوصول إلى ميزات الجهاز:

```typescript
// مثال: الكاميرا
import { Camera, CameraResultType } from '@capacitor/camera';

const photo = await Camera.getPhoto({
  resultType: CameraResultType.Uri,
});

// مثال: الموقع الجغرافي
import { Geolocation } from '@capacitor/geolocation';

const coordinates = await Geolocation.getCurrentPosition();

// مثال: الملفات
import { Filesystem, Directory, Encoding } from '@capacitor/filesystem';

await Filesystem.writeFile({
  path: 'secrets/text.txt',
  data: 'Hello World!',
  directory: Directory.Documents,
  encoding: Encoding.UTF8,
});
```

### الإذن (Permissions)

تأكد من إضافة الأذونات المطلوبة في `android/app/src/main/AndroidManifest.xml`:

```xml
<uses-permission android:name="android.permission.CAMERA" />
<uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
<uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
<uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
```

---

## 🐛 استكشاف الأخطاء

### المشكلة: "Gradle sync failed"

**الحل:**
1. تأكد من تثبيت JDK 17 أو أعلى
2. في Android Studio: `File` → `Invalidate Caches` → `Invalidate and Restart`
3. حاول مجدداً

### المشكلة: "Cannot connect to server"

**الحل:**
1. تأكد من تشغيل الخادم على المنفذ 3000
2. تأكد من عنوان IP الصحيح في `capacitor.config.ts`
3. تأكد من أن الجهاز والخادم على نفس الشبكة
4. تحقق من جدار الحماية (Firewall)

### المشكلة: "App crashes on startup"

**الحل:**
1. افتح `Logcat` في Android Studio
2. ابحث عن رسائل الخطأ
3. تحقق من وجود جميع الأذونات المطلوبة
4. تأكد من أن الخادم يعمل بشكل صحيح

### المشكلة: "Build failed"

**الحل:**
1. تأكد من وجود جميع المتطلبات
2. حاول `Clean Project` ثم `Rebuild`
3. احذف مجلد `build` و `gradle` وحاول مجدداً

---

## 📚 موارد إضافية

- [Capacitor Documentation](https://capacitorjs.com/docs)
- [Android Developer Guide](https://developer.android.com/guide)
- [Android Studio Documentation](https://developer.android.com/studio/intro)
- [Gradle Documentation](https://gradle.org/guides/)

---

## 💡 نصائح مهمة

1. **استخدم محاكي أولاً:** أسهل من جهاز حقيقي في البداية
2. **احفظ المفتاح:** احفظ مفتاح التوقيع في مكان آمن
3. **اختبر على أجهزة مختلفة:** تأكد من التوافقية
4. **راقب الأداء:** استخدم Android Profiler
5. **اقرأ السجلات:** Logcat هو أفضل صديق لك

---

**آخر تحديث:** يونيو 2026
**الإصدار:** 1.0.0
