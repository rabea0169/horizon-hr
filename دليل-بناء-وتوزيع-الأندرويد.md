# دليل بناء وتوزيع تطبيق Android - Horizon HR

## 📋 المحتويات
1. [البناء الأساسي](#البناء-الأساسي)
2. [بناء APK](#بناء-apk)
3. [بناء App Bundle](#بناء-app-bundle)
4. [التوقيع الرقمي](#التوقيع-الرقمي)
5. [النشر على Google Play](#النشر-على-google-play)
6. [التوزيع المباشر](#التوزيع-المباشر)

---

## 🏗️ البناء الأساسي

### الخطوة 1: بناء الويب

```bash
# في مجلد المشروع الرئيسي
npm run build
```

هذا ينشئ مجلد `dist/public` يحتوي على ملفات الويب المبنية.

### الخطوة 2: مزامنة Capacitor

```bash
# مزامنة الملفات مع مشروع Android
npx cap sync android

# أو إذا لم تكن قد أضفت Android من قبل
npx cap add android
```

### الخطوة 3: فتح في Android Studio

```bash
# افتح مشروع Android في Android Studio
npx cap open android
```

### الخطوة 4: انتظر Gradle Sync

في Android Studio:
1. انتظر انتهاء Gradle sync (قد يستغرق عدة دقائق)
2. إذا حدثت أخطاء، اضغط على `Sync Now` مرة أخرى

---

## 📦 بناء APK

### الطريقة الأولى: من Android Studio (الأسهل)

1. في Android Studio، اذهب إلى: `Build` → `Build Bundle(s) / APK(s)` → `Build APK(s)`
2. انتظر انتهاء البناء (قد يستغرق 5-10 دقائق)
3. ستظهر إشعارات في الأسفل
4. اضغط على `Locate` لفتح مجلد الملفات

الملف الناتج: `android/app/release/app-release.apk` أو `android/app/debug/app-debug.apk`

### الطريقة الثانية: من سطر الأوامر

```bash
# انتقل إلى مجلد Android
cd android

# بناء APK Debug (للاختبار)
./gradlew assembleDebug

# بناء APK Release (للإنتاج)
./gradlew assembleRelease

# أو استخدم على Windows
gradlew.bat assembleRelease
```

الملفات الناتجة:
- Debug: `app/build/outputs/apk/debug/app-debug.apk`
- Release: `app/build/outputs/apk/release/app-release.apk`

### الطريقة الثالثة: بناء متعدد الهندسات (ABI)

```bash
# بناء APK منفصل لكل هندسة معمارية
./gradlew assembleRelease

# أو بناء APK واحد يدعم جميع الهندسات
./gradlew assembleRelease -PabiBuild=universal
```

---

## 📱 بناء App Bundle

### الطريقة الأولى: من Android Studio

1. اذهب إلى: `Build` → `Build Bundle(s) / APK(s)` → `Build Bundle(s)`
2. انتظر انتهاء البناء
3. اضغط على `Locate` لفتح مجلد الملفات

الملف الناتج: `android/app/release/app-release.aab`

### الطريقة الثانية: من سطر الأوامر

```bash
# انتقل إلى مجلد Android
cd android

# بناء App Bundle
./gradlew bundleRelease

# أو على Windows
gradlew.bat bundleRelease
```

الملف الناتج: `app/build/outputs/bundle/release/app-release.aab`

---

## 🔐 التوقيع الرقمي

### إنشاء مفتاح التوقيع (Keystore)

#### الطريقة الأولى: من Android Studio

1. اذهب إلى: `Build` → `Generate Signed Bundle / APK`
2. اختر `APK` أو `Bundle`
3. اضغط على `Create new...` تحت `Key store path`
4. ملأ البيانات:
   - **Key store path:** اختر مكان حفظ الملف (مثل `C:\Users\YourName\release-key.keystore`)
   - **Password:** ضع كلمة مرور قوية
   - **Confirm:** أعد كتابة كلمة المرور
   - **Key alias:** ضع اسم المفتاح (مثل `horizon-key`)
   - **Key password:** ضع كلمة مرور للمفتاح
   - **Validity:** 25 سنة (أو أكثر)
   - **First and Last Name:** ضع اسمك
   - **Organizational Unit:** ضع اسم الشركة
   - **Organization:** ضع اسم الشركة
   - **City or Locality:** ضع المدينة
   - **State or Province:** ضع المحافظة
   - **Country Code:** ضع رمز الدولة (مثل `EG` لمصر)

5. اضغط `OK`
6. اختر `release` build type
7. اضغط `Next` ثم `Finish`

#### الطريقة الثانية: من سطر الأوامر

```bash
# على Windows (PowerShell)
keytool -genkey -v -keystore release-key.keystore -keyalg RSA -keysize 2048 -validity 10000 -alias horizon-key

# على Linux/macOS
keytool -genkey -v -keystore release-key.keystore -keyalg RSA -keysize 2048 -validity 10000 -alias horizon-key
```

ستُطلب منك البيانات التالية:
- كلمة مرور Keystore
- كلمة مرور المفتاح
- بيانات شخصية

### استخدام المفتاح في البناء

في `android/app/build.gradle`:

```gradle
android {
    ...
    signingConfigs {
        release {
            keyAlias 'horizon-key'
            keyPassword 'your_key_password'
            storeFile file('path/to/release-key.keystore')
            storePassword 'your_store_password'
        }
    }

    buildTypes {
        release {
            signingConfig signingConfigs.release
            minifyEnabled true
            shrinkResources true
            proguardFiles getDefaultProguardFile('proguard-android-optimize.txt'), 'proguard-rules.pro'
        }
    }
}
```

### التحقق من التوقيع

```bash
# تحقق من توقيع APK
jarsigner -verify -verbose app-release.apk

# أو استخدم apksigner
apksigner verify --print-certs app-release.apk
```

---

## 🌐 النشر على Google Play

### الخطوة 1: إنشاء حساب Google Play Developer

1. اذهب إلى [Google Play Console](https://play.google.com/console)
2. اضغط على `Create account`
3. ادفع رسم التسجيل ($25)
4. ملأ بيانات الحساب والشركة

### الخطوة 2: إنشاء تطبيق جديد

1. في Google Play Console، اضغط على `Create app`
2. أدخل اسم التطبيق: `Horizon HR`
3. اختر الفئة: `Business` أو `Productivity`
4. اضغط `Create`

### الخطوة 3: ملأ بيانات التطبيق

1. **App details:**
   - اسم التطبيق
   - الوصف القصير
   - الوصف الكامل
   - الكلمات المفتاحية
   - فئة التطبيق
   - التقييم العمري

2. **Graphics:**
   - أيقونة التطبيق (512x512 px)
   - لقطات الشاشة (2-8 لقطات)
   - صورة الميزة (1024x500 px)
   - صورة الفيديو (اختياري)

3. **Content rating:**
   - ملأ استبيان التقييم

4. **Pricing and distribution:**
   - السعر (مجاني أو مدفوع)
   - الدول المسموح بها

### الخطوة 4: رفع App Bundle

1. اذهب إلى: `Release` → `Production`
2. اضغط على `Create new release`
3. اضغط على `Browse files` وحمّل `app-release.aab`
4. أضف ملاحظات الإصدار
5. اضغط `Review release`
6. اضغط `Start rollout to Production`

### الخطوة 5: الانتظار للمراجعة

- Google Play ستراجع التطبيق (عادة 24-48 ساعة)
- ستتلقى بريد إلكتروني عند الموافقة أو الرفض
- بعد الموافقة، سيكون التطبيق متاحاً للتحميل

---

## 📤 التوزيع المباشر

### إرسال APK عبر البريد الإلكتروني

```bash
# ابحث عن ملف APK
# android/app/release/app-release.apk

# أرسله عبر البريد الإلكتروني أو أي خدمة مشاركة
```

### استخدام Firebase App Distribution

1. تثبيت Firebase CLI:
```bash
npm install -g firebase-tools
```

2. تسجيل الدخول:
```bash
firebase login
```

3. رفع APK:
```bash
firebase appdistribution:distribute app-release.apk \
  --app=com.horizonsolutions.hr:android \
  --release-notes="Version 1.0.0" \
  --testers="email1@example.com,email2@example.com"
```

### استخدام GitHub Releases

1. أنشئ repository على GitHub
2. أضف ملف APK إلى Releases
3. شارك الرابط مع المستخدمين

---

## 🧪 الاختبار قبل النشر

### اختبار محلي

```bash
# تثبيت APK على جهاز أو محاكي
adb install app-release.apk

# أو
adb install -r app-release.apk  # إعادة التثبيت
```

### اختبار على أجهزة متعددة

1. استخدم Firebase Test Lab
2. اختبر على أجهزة مختلفة وإصدارات Android مختلفة
3. تأكد من التوافقية

### اختبار الأداء

1. استخدم Android Profiler
2. راقب استخدام الذاكرة والـ CPU
3. تأكد من عدم وجود memory leaks

---

## 🔄 تحديث التطبيق

### زيادة رقم الإصدار

في `android/app/build.gradle`:

```gradle
android {
    ...
    defaultConfig {
        ...
        versionCode 2  // زيادة هذا الرقم
        versionName "1.1.0"  // تحديث اسم الإصدار
    }
}
```

### بناء نسخة جديدة

```bash
# بناء الويب
npm run build

# مزامنة Capacitor
npx cap sync android

# بناء APK/Bundle جديد
cd android
./gradlew assembleRelease
```

---

## 📊 مراقبة الإصدار

### في Google Play Console

1. اذهب إلى `Analytics`
2. راقب عدد التحميلات والتثبيتات
3. اقرأ تقييمات المستخدمين
4. راقب معدل الأخطاء

### استخدام Firebase Crashlytics

1. أضف Firebase إلى المشروع
2. ثبّت Crashlytics SDK
3. راقب الأخطاء والأعطال

---

## 🐛 استكشاف الأخطاء

### المشكلة: "Build failed"

**الحل:**
1. تأكد من تثبيت JDK 17 أو أعلى
2. حاول `Clean Project` ثم `Rebuild`
3. احذف مجلد `build` وحاول مجدداً

### المشكلة: "Gradle sync failed"

**الحل:**
1. اضغط على `Sync Now`
2. تحقق من الاتصال بالإنترنت
3. في Android Studio: `File` → `Invalidate Caches` → `Invalidate and Restart`

### المشكلة: "App crashes on startup"

**الحل:**
1. افتح Logcat في Android Studio
2. ابحث عن رسائل الخطأ
3. تأكد من أن الخادم يعمل بشكل صحيح
4. تحقق من عنوان IP في `capacitor.config.ts`

### المشكلة: "Cannot connect to server"

**الحل:**
1. تأكد من تشغيل الخادم على المنفذ 3000
2. تأكد من عنوان IP الصحيح
3. تأكد من أن الجهاز والخادم على نفس الشبكة
4. تحقق من جدار الحماية

---

## 💾 النسخ الاحتياطية

### حفظ مفتاح التوقيع

```bash
# احفظ ملف keystore في مكان آمن
# لا تفقده! ستحتاجه لتحديثات التطبيق المستقبلية

# يمكنك حفظه في:
# - محرك أقراص خارجي
# - خدمة سحابية آمنة (Google Drive, OneDrive)
# - مخزن كلمات مرور آمن
```

### توثيق البيانات

احفظ في مكان آمن:
- كلمة مرور Keystore
- كلمة مرور المفتاح
- اسم المفتاح (alias)
- مسار الملف

---

## 📚 موارس إضافية

- [Android Developer Guide](https://developer.android.com/guide)
- [Google Play Console Help](https://support.google.com/googleplay/android-developer)
- [Capacitor Android Guide](https://capacitorjs.com/docs/android)
- [Gradle Documentation](https://gradle.org/guides/)

---

## 💡 نصائح مهمة

1. **احفظ المفتاح:** لا تفقد ملف keystore
2. **اختبر قبل النشر:** تأكد من عدم وجود أخطاء
3. **اقرأ السجلات:** Logcat هو أفضل صديق لك
4. **راقب التقييمات:** استمع إلى ملاحظات المستخدمين
5. **حدّث بانتظام:** أضف ميزات جديدة وإصلاحات الأخطاء

---

**آخر تحديث:** يونيو 2026
**الإصدار:** 1.0.0
