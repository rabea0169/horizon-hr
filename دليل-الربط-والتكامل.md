# دليل الربط والتكامل - Horizon HR

## 📋 المحتويات
1. [معمارية النظام](#معمارية-النظام)
2. [الربط بين التطبيقات](#الربط-بين-التطبيقات)
3. [تكوين الخادم](#تكوين-الخادم)
4. [تكوين قاعدة البيانات](#تكوين-قاعدة-البيانات)
5. [المصادقة والأمان](#المصادقة-والأمان)
6. [الاختبار والتشخيص](#الاختبار-والتشخيص)

---

## 🏗️ معمارية النظام

```
┌─────────────────────────────────────────────────────────────────┐
│                      Horizon HR System                           │
├─────────────────────────────────────────────────────────────────┤
│                                                                   │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐           │
│  │  Desktop     │  │   Web        │  │   Mobile     │           │
│  │  (Electron)  │  │  (Browser)   │  │  (Android)   │           │
│  └──────┬───────┘  └──────┬───────┘  └──────┬───────┘           │
│         │                 │                 │                    │
│         └─────────────────┼─────────────────┘                    │
│                           │                                      │
│                    ┌──────▼──────┐                               │
│                    │  Hono API   │                               │
│                    │  (Port 3000)│                               │
│                    └──────┬──────┘                               │
│                           │                                      │
│                    ┌──────▼──────────┐                           │
│                    │  MySQL Database │                           │
│                    │  (Port 3306)    │                           │
│                    └─────────────────┘                           │
│                                                                   │
└─────────────────────────────────────────────────────────────────┘
```

### المكونات:

| المكون | الوصف | المنفذ | البيئة |
|-------|-------|--------|-------|
| **Desktop (Electron)** | تطبيق سطح المكتب | N/A | Windows/Linux/macOS |
| **Web (Browser)** | تطبيق الويب | 3000 | أي متصفح |
| **Mobile (Android)** | تطبيق الهاتف الذكي | N/A | Android 7+ |
| **Hono API** | خادم الـ API | 3000 | Node.js |
| **MySQL Database** | قاعدة البيانات | 3306 | MySQL 8.0+ |

---

## 🔗 الربط بين التطبيقات

### 1. الخادم المركزي (Hono API)

جميع التطبيقات تتصل بخادم Hono المركزي على المنفذ 3000.

**الخادم يوفر:**
- API endpoints عبر tRPC
- المصادقة والتفويض
- الوصول إلى قاعدة البيانات
- معالجة الأخطاء والتحقق

### 2. تطبيق Desktop (Electron)

```
Electron App
    ↓
React Frontend (dist/public)
    ↓
Hono API (localhost:3000)
    ↓
MySQL Database
```

**الخطوات:**
1. بناء الـ React app: `npm run build`
2. تشغيل Electron: `npm run electron`
3. يتصل تلقائياً بـ localhost:3000

### 3. تطبيق الويب (Browser)

```
Browser (http://localhost:3000)
    ↓
React Frontend
    ↓
Hono API (localhost:3000)
    ↓
MySQL Database
```

**الخطوات:**
1. بناء الـ React app: `npm run build`
2. تشغيل الخادم: `npm run start`
3. افتح المتصفح على: `http://localhost:3000`

### 4. تطبيق الهاتف (Android)

```
Android App
    ↓
React Native (Capacitor)
    ↓
Hono API (192.168.1.100:3000)
    ↓
MySQL Database
```

**الخطوات:**
1. بناء الـ React app: `npm run build`
2. مزامنة Capacitor: `npx cap sync android`
3. فتح في Android Studio: `npx cap open android`
4. تشغيل على محاكي أو جهاز حقيقي

---

## ⚙️ تكوين الخادم

### 1. ملف الإعدادات (.env)

```env
# Database
DATABASE_URL=mysql://hr_user:password@localhost:3306/horizon_hr

# Backend
APP_ID=horizon-hr-app
APP_SECRET=your_secret_key

# Frontend (OAuth)
VITE_KIMI_AUTH_URL=https://auth.kimi.com
VITE_APP_ID=your_app_id

# Backend (OAuth)
KIMI_AUTH_URL=https://auth.kimi.com
KIMI_OPEN_URL=https://open.kimi.com
```

### 2. بدء الخادم

**في وضع التطوير:**
```bash
npm run dev
```

**في وضع الإنتاج:**
```bash
npm run build
npm run start
```

### 3. التحقق من الخادم

```bash
# افتح المتصفح على:
http://localhost:3000

# أو استخدم curl:
curl http://localhost:3000/api/trpc/auth.me
```

---

## 🗄️ تكوين قاعدة البيانات

### 1. إنشاء قاعدة البيانات

```sql
CREATE DATABASE horizon_hr 
CHARACTER SET utf8mb4 
COLLATE utf8mb4_unicode_ci;
```

### 2. إنشاء مستخدم

```sql
CREATE USER 'hr_user'@'localhost' IDENTIFIED BY 'strong_password';
GRANT ALL PRIVILEGES ON horizon_hr.* TO 'hr_user'@'localhost';
FLUSH PRIVILEGES;
```

### 3. تطبيق الهجرات

```bash
# توليد ملفات الهجرة
npm run db:generate

# تطبيق الهجرات
npm run db:push
```

### 4. التحقق من الاتصال

```bash
# اختبر الاتصال
mysql -u hr_user -p -h localhost -e "SELECT 1;"

# أو من Node.js
npm run db:generate
```

---

## 🔐 المصادقة والأمان

### 1. نظام المصادقة

المشروع يستخدم **JWT (JSON Web Tokens)** للمصادقة.

**الخطوات:**
1. المستخدم يسجل دخول
2. الخادم ينشئ JWT token
3. التطبيق يحفظ الـ token في localStorage
4. كل طلب يرسل الـ token في header `Authorization`

### 2. الحسابات الافتراضية

| المستخدم | كلمة المرور | الدور |
|---------|----------|------|
| admin | admin123 | مدير النظام |
| supervisor | super123 | المشرف |
| accountant | acc123 | محاسب |
| worker | work123 | عامل |

**ملاحظة:** هذه الحسابات موجودة في `api/auth-router.ts`. في الإنتاج، يجب نقلها إلى قاعدة البيانات.

### 3. تأمين الاتصال

**في الإنتاج:**
- استخدم HTTPS بدلاً من HTTP
- استخدم متغيرات بيئة قوية
- فعّل CORS بشكل صحيح
- استخدم HTTPS للاتصال بـ MySQL

**في التطوير:**
- استخدم HTTP (مقبول)
- استخدم متغيرات بيئة بسيطة
- فعّل CORS للـ localhost

### 4. تحديث APP_SECRET

```bash
# توليد مفتاح سري قوي
# على Windows (PowerShell):
[Convert]::ToBase64String([System.Text.Encoding]::UTF8.GetBytes((New-Guid).ToString())) | Out-Host

# على Linux/macOS:
openssl rand -base64 32
```

ثم أضفه إلى ملف `.env`:
```env
APP_SECRET=your_generated_secret_here
```

---

## 🧪 الاختبار والتشخيص

### 1. اختبار الاتصال بقاعدة البيانات

```bash
# استخدم MySQL CLI
mysql -u hr_user -p -h localhost horizon_hr -e "SELECT COUNT(*) FROM users;"

# أو استخدم Node.js
npm run db:generate
```

### 2. اختبار الخادم

```bash
# افتح المتصفح
http://localhost:3000

# أو استخدم curl
curl http://localhost:3000/api/trpc/auth.me

# يجب أن ترى رسالة خطأ تتعلق بعدم المصادقة
# هذا يعني أن الخادم يعمل بشكل صحيح
```

### 3. اختبار تطبيق Desktop

```bash
# بناء التطبيق
npm run build

# تشغيل Electron
npm run electron

# يجب أن يفتح نافذة التطبيق
```

### 4. اختبار تطبيق الويب

```bash
# بناء التطبيق
npm run build

# تشغيل الخادم
npm run start

# افتح المتصفح على http://localhost:3000
```

### 5. اختبار تطبيق Android

```bash
# بناء التطبيق
npm run build

# مزامنة Capacitor
npx cap sync android

# فتح في Android Studio
npx cap open android

# تشغيل على محاكي أو جهاز حقيقي
```

### 6. عرض السجلات (Logs)

**في الخادم:**
```bash
# السجلات تظهر في نفس النافذة
# ابحث عن رسائل الخطأ
```

**في المتصفح:**
```bash
# افتح Developer Tools (F12)
# اذهب إلى Console
# ابحث عن رسائل الخطأ
```

**في Android:**
```bash
# في Android Studio
# اذهب إلى Logcat
# ابحث عن رسائل الخطأ
```

---

## 🚀 خطوات البدء السريع

### للتطوير المحلي:

```bash
# 1. تثبيت المتطلبات
npm install

# 2. إعداد قاعدة البيانات
npm run db:generate
npm run db:push

# 3. تشغيل الخادم
npm run dev

# 4. في نافذة أخرى، تشغيل Electron
npm run electron

# أو افتح المتصفح على http://localhost:3000
```

### للإنتاج:

```bash
# 1. تثبيت المتطلبات
npm install

# 2. إعداد قاعدة البيانات
npm run db:generate
npm run db:push

# 3. بناء التطبيق
npm run build

# 4. تشغيل الخادم
npm run start
```

---

## 📊 مراقبة الأداء

### استخدام MySQL Workbench

1. افتح MySQL Workbench
2. اتصل بـ MySQL Server
3. اذهب إلى `Performance` → `Dashboard`
4. راقب استخدام الموارد

### استخدام Chrome DevTools

1. افتح المتصفح على التطبيق
2. اضغط F12
3. اذهب إلى `Network` لمراقبة الطلبات
4. اذهب إلى `Performance` لتسجيل الأداء

### استخدام Android Profiler

1. في Android Studio
2. اذهب إلى `View` → `Tool Windows` → `Profiler`
3. راقب استخدام الذاكرة والـ CPU

---

## 💡 نصائح مهمة

1. **استخدم نفس الإصدار من Node.js:** تأكد من استخدام نفس الإصدار في جميع الأجهزة
2. **احفظ نسخة احتياطية:** قبل تطبيق أي تغييرات على قاعدة البيانات
3. **استخدم Git:** لتتبع التغييرات والتعاون
4. **اختبر على أجهزة مختلفة:** تأكد من التوافقية
5. **اقرأ السجلات:** السجلات هي أفضل صديق لك عند استكشاف الأخطاء

---

## 📚 موارد إضافية

- [Hono Documentation](https://hono.dev)
- [tRPC Documentation](https://trpc.io)
- [Drizzle ORM Documentation](https://orm.drizzle.team)
- [Capacitor Documentation](https://capacitorjs.com)
- [MySQL Documentation](https://dev.mysql.com/doc/)

---

**آخر تحديث:** يونيو 2026
**الإصدار:** 1.0.0
